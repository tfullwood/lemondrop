{
  "name": "lemondrop",
  "version": "0.0.1",
  "description": "CLI tool to split and prettify large JSON files.",
  "main": "index.js",
  "bin": {
    "lemondrop": "bin/lemondrop"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "pkg": "pkg bin/lemondrop --output './bin/build/lemondrop'"
  },
  "author": "Trevor Fullwood",
  "license": "ISC",
  "dependencies": {
    "chalk": "^2.4.2",
    "yargs": "^14.0.0"
  }
}
